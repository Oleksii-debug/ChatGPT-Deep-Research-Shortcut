{
  "name": "accessible-tools-picker",
  "version": "0.5.0",
  "private": true,
  "description": "Tests and packaging metadata for the accessible verified tools picker.",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}

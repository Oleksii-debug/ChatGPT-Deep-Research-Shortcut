{
  "name": "chatgpt-deep-research-shortcut",
  "version": "0.1.0",
  "private": true,
  "description": "Tests and packaging metadata for the ChatGPT Deep Research Shortcut Chrome extension.",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
